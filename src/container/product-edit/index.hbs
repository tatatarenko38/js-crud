{{!
Це код container файлу для index сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
}}

<div>
  <section>
    <h1 class="title">Редагування товару</h1>
    <div class='form'>
      <h2 class='form__title'>Інформація про товар</h2>
      {{#with data.product}}
      <form method='POST' action='/product-edit'>
        <div class='form__content'>
          <div class="form__product form__edit">
            <div class='form__field'>
              <label for='name' class='form__label'>Назва
                товару</label>
              <input type='text' name='name' class='form__input' value="{{name}}" />
            </div>
            <div class='form__field'>
              <label for='price' class='form__label'>Ціна</label>
              <input type='number' name='price' class='form__input' value="{{price}}" />
            </div>
            <div class='form__field'>
              <label for='ID' class='form__label'>ID</label>
              <input type='number' name='id' class='form__input' value="{{id}}" />

            </div>
          </div>
          <div class='form__field'>
            <label for='description' class='form__label'>Короткий опис товару</label>
            <input type='text' name='description' class='form__input' value="{{description}}" />
            <div class="form__text">Відображається у картці товару</div>
          </div>
          <div class="form__button-edit">

            <button type="submit" class='form__button'><span class="button__text">Зберегти
                оновлення</span></button>


            <a href="/product-delete?id={{id}}" type="button" class='form__button form__button-delete '><span
                class="button__text">Видалити
                товар</span></a>

          </div>
        </div>
      </form>
      {{/with}}
    </div>
  </section>
</div>

{{!
Цей код містить лише те, що буде виводитись в

<body>
  Адже наш layout/default.hbs має вже структуру HTML
  }}